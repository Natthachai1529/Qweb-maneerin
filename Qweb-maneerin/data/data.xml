<odoo>
    <record id="action_print_contract" model="ir.actions.server">
        <field name="name">Print Contract</field>
        <field name="model_id" ref="contract.model_contract"/> <!-- แก้ไขให้ตรงกับ External ID ที่ถูกต้อง -->
        <field name="binding_model_id" ref="contract.model_contract"/> <!-- แก้ไขให้ตรงกับ External ID ที่ถูกต้อง -->
        <field name="binding_view_types">form,list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_print_contract()</field>
    </record>
</odoo>
