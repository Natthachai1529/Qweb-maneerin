<odoo>
    <!-- Paper Format Definition -->
    <record id="paperformat_contract_report" model="report.paperformat">
        <field name="name">Contract A4 Format</field>
        <field name="default" eval="True"/>
        <field name="format">A4</field>
        <field name="page_height">0</field>
        <field name="page_width">0</field>
        <field name="orientation">Portrait</field>
        <field name="margin_top">15</field>
        <field name="margin_bottom">25</field>
        <field name="margin_left">15</field>
        <field name="margin_right">15</field>
        <field name="header_line" eval="False"/>
        <field name="header_spacing">0</field>
        <field name="dpi">90</field>
    </record>

    <!-- QWeb Template for Contract Report -->
    <template id="report_contract_document">
        <t t-call="web.basic_layout">
            <div class="page">
                <h2>Contract Report</h2>
                <p><strong>เลขที่สัญญา:</strong> <t t-esc="doc.contract_no"/></p>
                <p><strong>วันที่ติดต่อ:</strong> <t t-esc="doc.contact_date"/></p>
                <p><strong>ผู้ขาย:</strong> <t t-esc="doc.seller.name"/></p>
                <h3>ผู้จะซื้อ:</h3>
                <t t-foreach="doc.buyer_ids" t-as="buyer">
                    <p><strong>ชื่อ:</strong> <t t-esc="buyer.partner_id.name"/> | 
                       <strong>อายุ:</strong> <t t-esc="buyer.age"/> | 
                       <strong>สัญชาติ:</strong> <t t-esc="buyer.nation"/> | 
                       <strong>ที่อยู่:</strong> <t t-esc="buyer.street"/> | 
                       <strong>โทรศัพท์:</strong> <t t-esc="buyer.phone"/></p>
                </t>
                <h3>ผู้รับสิทธิ์:</h3>
                <t t-foreach="doc.beneficiary_ids" t-as="beneficiary">
                    <p><strong>ชื่อ:</strong> <t t-esc="beneficiary.partner_id.name"/> | 
                       <strong>อายุ:</strong> <t t-esc="beneficiary.age"/> | 
                       <strong>สัญชาติ:</strong> <t t-esc="beneficiary.nation"/> | 
                       <strong>ที่อยู่:</strong> <t t-esc="beneficiary.street"/> | 
                       <strong>โทรศัพท์:</strong> <t t-esc="beneficiary.phone"/></p>
                </t>
            </div>
        </t>
    </template>

    <!-- Report Action -->
    <record id="action_report_contract" model="ir.actions.report">
        <field name="name">Contract Report</field>
        <field name="model">maneerincode.code</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">your_module_name.report_contract_document</field> <!-- เปลี่ยนเป็นชื่อโมดูลของคุณ -->
        <field name="paperformat_id" ref="your_module_name.paperformat_contract_report"/> <!-- เปลี่ยนเป็น id ของฟอร์แมตที่สร้างไว้ -->
        <field name="binding_model_id" ref="your_module_name.model_maneerincode_code"/> <!-- เปลี่ยนเป็นโมเดลที่คุณใช้ -->
    </record>

    <!-- Menu Item for Report -->
    <menuitem id="menu_report_contract" name="Contract Report" parent="your_module_name.menu_root" action="action_report_contract"/>
</odoo>
