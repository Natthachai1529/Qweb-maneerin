<record id="action_report_contract" model="ir.actions.report">
    <field name="name">Print Contract</field>
    <field name="model">maneerincode.code</field>
    <field name="report_type">qweb-pdf</field>
    <field name="report_name">maneerin.report_maneerin_template</field> <!-- เปลี่ยนเป็นชื่อที่ถูกต้อง -->
    <field name="report_file">maneerin.report_maneerin_template</field> <!-- เปลี่ยนเป็นชื่อที่ถูกต้อง -->
    <field name="print_report_name">Contract - ${object.name}</field>
    <field name="paperformat_id" ref="your_module_name.paperformat_report"/> <!-- เปลี่ยนเป็นชื่อที่ถูกต้อง -->
    <field name="binding_model_id" ref="your_module_name.model_maneerincode_code"/> <!-- เปลี่ยนเป็นชื่อที่ถูกต้อง -->
    <field name="binding_type">report</field>
</record>
